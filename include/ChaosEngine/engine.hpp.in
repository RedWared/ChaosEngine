#ifndef ENGINE_HPP
#define ENGINE_HPP
#define CHAOS_VERSION "${PROJECT_VERSION_STRING}"

#include <iostream>
#include <string>

using namespace std;

class Engine {
public:
	Engine();
	~Engine();
	void log(string msg);
	void error(string error, int errCode=0, bool fatal=false);
	void CreateWin(string title="Chaos Game", int x_pos=0, int y_pos=0, int width=800, int height=600);
};

#endif // ENGINE_HPP
